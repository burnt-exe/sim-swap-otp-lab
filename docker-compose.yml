services:
  sim-swap-otp-lab:
    build: .
    container_name: sim-swap-otp-lab
    ports:
      - "8000:8000"
    volumes:
      - .:/workspace
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
